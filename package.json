{
  "name": "epoch-core",
  "version": "0.1.0",
  "description": "Structured computation of classical astrological systems from birth data.",
  "main": "src/index.ts",
  "license": "GPL-3.0-or-later",
  "type": "commonjs",
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "clean": "node ./scripts/clean.mjs",
    "verify": "npm run clean && tsc -p tsconfig.verify.json && node ./dist-verify/tests/verify.js",
    "check": "npm run verify"
  },
  "dependencies": {
    "iztro": "^2.5.8",
    "lunar-typescript": "^1.8.6",
    "swisseph-wasm": "^0.0.5"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
